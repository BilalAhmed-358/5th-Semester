CREATE TABLE student_tb(
    std_id int,
    std_name varchar(20)
);

desc student_tb;

CREATE TABLE EMPLOYEE(
    EMP_ID NUMBER NOT NULL,
    EMP_NAME VARCHAR(20) NOT NULL
);

DESC EMPLOYEE;

CREATE TABLE EMPLOYEE2(
    EMP_ID NUMBER NOT NULL,
    EMP_NAME VARCHAR(20) NOT NULL,
    SALARY NUMBER,
    CONSTRAINT NN NOT NULL(SALARY)
);

ALTER TABLE EMPLOYEE ADD POST VARCHAR(20);
DESC EMPLOYEE;

CREATE TABLE EMPLOYEE3(
    EMP_ID NUMBER NOT NULL UNIQUE,
    EMP_NAME VARCHAR(20) NOT NULL
);
DESC EMPLOYEE3

CREATE TABLE EMPLOYEE5(
    EMP_ID NUMBER NOT NULL UNIQUE,
    EMP_NAME VARCHAR(20) NOT NULL,
    CONSTRAINT CK CHECK(EMP_NAME = INITCAP(EMP_NAME))
);
DESC EMPLOYEE5;

CREATE TABLE EMPLOYEE6(
    EMP_ID NUMBER PRIMARY KEY,
    EMP_NAME VARCHAR(20) UNIQUE,
    SALARY NUMBER,
    COMMISSION NUMBER DEFAULT 0.1
);
DESC EMPLOYEE6;


CREATE TABLE JOBS(
    JOB_ID NUMBER PRIMARY KEY,
    JOB_TITLE VARCHAR(20)
);
DESC JOBS;

CREATE TABLE EMPLOYEE7(
    EMP_ID NUMBER PRIMARY KEY,
    EMP_NAME VARCHAR(20) UNIQUE,
    SALARY NUMBER,
    COMMISSION NUMBER DEFAULT 0.1,
    JOB_ID NUMBER,
    FOREIGN KEY (JOB_ID) REFERENCES JOBS(JOB_ID)
);
DESC EMPLOYEE7;

https://krunker.io/?game=BHN:d51gv
ALTER TABLE EMPLOYEE7 DROP COLUMN COMMISSION;
DESC EMPLOYEE7;

ALTER TABLE EMPLOYEE7 MODIFY EMP_ID VARCHAR(20);
DESC EMPLOYEE7;

DROP TABLE EMPLOYEE3;

ALTER TABLE EMPLOYEE4 RENAME TO TABLE1;
DESC TABLE1;
ALTER TABLE TABLE1 RENAME COLUMN EMP_ID TO ID;
DESC TABLE1;

ALTER TABLE TABLE1 RENAME COLUMN ID TO EMP_ID;

INSERT INTO TABLE1 (EMP_ID, EMP_NAME, SALARY) VALUES(19, 'ASHMAL', 20000);

INSERT INTO TABLE1 (EMP_ID, EMP_NAME, SALARY)
VALUES (20, 'ash', 20000), (21, 'al', 21000); -- error araha hai

select * from table1;


UPDATE HR.EMPLOYEES SET SALARY = 8000 WHERE SALARY < 5000 AND EMPLOYEE_ID = 105;

SELECT * FROM EMPLOYEES WHERE EMPLOYEE_ID = 105;

DELETE FROM HR.EMPLOYEES WHERE SALARY < 2000;