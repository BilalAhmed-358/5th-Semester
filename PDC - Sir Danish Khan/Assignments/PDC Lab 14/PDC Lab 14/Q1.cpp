#include <bits/stdc++.h>
#include <mpi.h>
using namespace std;

int main(int argc, char *argv[])
{
    MPI_Init(&argc, &argv);
    fstream file;
    file.open("test.txt");
    map <
        MPI_Finalize();
}